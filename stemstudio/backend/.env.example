# StemStudio backend environment variables
# Copy to .env and adjust values

# Directory for temporary job files (default: /tmp/stemstudio)
TMP_ROOT=/tmp/stemstudio

# Auto-delete jobs older than this many hours (default: 1)
MAX_JOB_AGE_HOURS=1
